require 'spec_helper'

describe Five::Star do
  it 'has a version number' do
    expect(Five::Star::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
